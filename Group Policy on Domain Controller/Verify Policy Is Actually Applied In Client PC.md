On **PC1**:

`gpresult /r`
`gpresult /?`

OR

`rsop.msc`

OR

`set l`

Check:

- **Applied GPOs (User Settings)** → must show your GPO

If it doesn’t:

- Wrong OU
    
- Wrong GPO scope
    
- Security filtering issue
    
- Or loopback confusion